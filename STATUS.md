# STATUS

Update at end of every session — source of truth, not memory/chat history.

## Mac
- Build (CI): ✅ passing — 2026-07-04 (run 28695377642)
- Self-test (AX/Clipboard): ✅ PASS
- BLE hardware (real pen): 🔄 in progress
- Notarization: ❌ blocked — needs Developer ID enrollment ($99/yr)
- DMG: ✅ built, signed (self-signed cert only)

## Windows
- Build (CI): ✅ passing — 2026-07-04 (run 28695377642, fixed uia_injector.rs compile error)
- Build (real PC): ⬜ never run
- BLE hardware (real pen): ⬜ untested
- Injection: UIA ValuePattern implemented; TextPattern write path removed (API doesn't support it) — falls back to clipboard+Ctrl-V for rich-text controls. Neither path tested on real hardware.
- Installer (.msi/.exe): ✅ generated by CI (11.1MB), never run/tested

## Known limitation
UIA injection on Windows only works via ValuePattern (standard inputs). Rich-text
controls (Word body, RichTextBox) always fall through to clipboard+Ctrl-V — this
is correct/expected, not a bug, since ITextRangeProvider has no write method.

## Next up
1. Physical Windows PC session — download CI installer artifact, run it, test BLE + injection (both ValuePattern and clipboard-fallback paths) with real pen
2. Real-pen BLE test on Mac
3. Developer ID enrollment → notarize Mac DMG

---
Last updated: 2026-07-04
